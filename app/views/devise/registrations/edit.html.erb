<div class="row content padding1">
    <%= render 'layouts/messages' %><br>
</div>
<div class="row content">
  <div class="col-xs-12 col-xs-offset-0 col-md-8 col-md-offset-2 text-left bottom-border padding4">
      <span style="display: inline-block; vertical-align: bottom;">
          <%= render "layouts/navbar" %>
      </span> 
  </div>
</div>
<div class="row content">
  <div class="col-xs-12 padding6">
      <center>
          <h3>Your Profile</h3><br>
          <%= image_tag(current_user.imgurl, :class => "user-photo")%><br>
          <div><%= current_user.name %></div>
          <div>
              <h3>Your Email:</h3>
              <%= current_user.email %><br><br>
              <%= render 'devise/passwords/edit_form' %>
              <h3>Looking to leave?</h3>
              <button>Delete My Account</button>
              <%= button_to "Cancel your account", registration_path(resource_name), data: { confirm: "Are you sure?" }, method: :delete %></p>
          </div>
        </center>
  </div>    
</div>









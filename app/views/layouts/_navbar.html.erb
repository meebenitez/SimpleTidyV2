<% if user_signed_in? %>
    <div class="dropbtn-holder">
        <div class="dropdown">
            <button class="dropbtn">☰</button>
            <div class="dropdown-content">
            <%= link_to 'Your Account', edit_user_registration_path %>
            <%= link_to "Logout", destroy_user_session_path, method: :delete  %>
            </div>
        </div>
    </div>
    <div class="list-name"><span class="list-name-html"><%= link_to "SimpleTidy", lists_path %></span></div>
<% else %>
    <div class="dropbtn-holder">
        <div class="dropdown">
            <button class="dropbtn">☰</button><h1><%= link_to "SimpleTidy", lists_path %></h1>
            <div class="dropdown-content">
                <%= link_to 'Login', new_user_session_path %>
            </div>
        </div>
    </div>
    <div class="list-name"><span class="list-name-html"><%= link_to "SimpleTidy", lists_path %></span></div>
<% end %>
